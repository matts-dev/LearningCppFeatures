
void static_dynamic_array(const int arraySize)
{
	//appears c++ 11 on VS doesn't allow stack dynamic? Maybe it is only a revision in C?
	//this is allowed in gcc on
	//int array[arraySize];
	//sizeof(array); //this couldn't possibly work.


}